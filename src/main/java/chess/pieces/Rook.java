package chess.pieces;

import chess.board.Square;

public class Rook extends Piece{
    public Rook(Square currentSquare, PieceColor color, String pieceName) {
        super(currentSquare, color, pieceName);
    }
}
